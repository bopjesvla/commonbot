public class Commonbot2 {
	/**
	 * The main function. This creates a control which then handles everything else.
	 * @param args Command line arguments
	 */
	public static void main(String[] args) {
		Control c = new Control();
	}
}